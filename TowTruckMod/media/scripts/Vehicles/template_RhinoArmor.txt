module Base
{
	template vehicle RhinoArmor
	{
		part Chevalier_Rhino_ArmorBullBar
		{
			model Default
			{
				file = Chevalier_Rhino_Bullbar,
			}
			
			area = Engine,
			category = Other,
			itemType = Base.RhinoArmor_BullBar_Item,
			mechanicRequireKey = false,
			
			table install
			{
                items
                {
                    1
                    {
                        type = Base.Wrench,
                        count = 1,
                        keep = true,
						equip = primary,
                    }
                }

                time = 1500,
                skills = Mechanics:2,
                test = Vehicles.InstallTest.Default,
				complete = rhinoArmor.InstallComplete.Bullbar,
            }

			table uninstall
			{
                items
                {
                   	1
                    {
                        type = Base.Wrench,
                        count = 1,
                        keep = true,
						equip = primary,
                    }
                }

                time = 1500,
                skills = Mechanics:1,
                test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Default,
            }

			lua
            {
				create = rhinoArmor.Create.Bullbar,
				update = rhinoArmor.Update.Bullbar,
			}
		}				
	}
}